/**
 * Checks if a string is in all lowercase letters.
 *
 * @param str - The input string to be checked.
 * @returns A boolean indicating whether the input string is in all lowercase letters.
 */
export const isLowerCase = (str: string): boolean => str === str.toLowerCase()
